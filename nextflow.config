// nextflow.config - minimal nf-core compatible
manifest {
    name            = 'nf-pipeline-demo'
    description     = 'Minimal pipeline pointing to main.nf'
    version         = '0.1.0'
    nextflowVersion = '>=20.10.0'
    mainScript      = 'main.nf'
}

params {
    // Only available parameter
    greeting = 'Hello, world!'
}